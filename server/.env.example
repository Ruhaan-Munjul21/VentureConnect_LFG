# Email Configuration
# Choose one of the following email providers:

# Option 1: SendGrid (Recommended - Easy to set up)
# Sign up at https://sendgrid.com/free/
# Get your API key from Settings > API Keys
SENDGRID_API_KEY=apikey
# Option 2: SMTP (Works with Gmail, Outlook, etc.)
# For Gmail: Enable 2FA and create an App Password
# Gmail SMTP: smtp.gmail.com, port 587
# Outlook SMTP: smtp-mail.outlook.com, port 587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Settings
EMAIL_FROM=noreply@ventrilinks.com
FRONTEND_URL=http://localhost:5173

# Database
DATABASE_URL=your_database_url
AIRTABLE_API_KEY=your_airtable_key
BASE_ID=your_base_id

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_here